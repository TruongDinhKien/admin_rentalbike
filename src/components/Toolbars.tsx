import { TopToolbar, ListButton, CreateButton } from "react-admin"

export const ActionButton = () => {
    return (
      <TopToolbar>
        <ListButton />
        <CreateButton />
      </TopToolbar>
    )
  }